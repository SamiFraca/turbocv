---
description: 
auto_execution_mode: 1
---
For each dependency listed in the given file:

Run the command:

pnpm view <package-name> version


to get the latest version of the package.

Update the dependency entry in the file to use that latest version.

After updating, install dependencies and run the project to confirm it works correctly with the new version.

Repeat this process until all dependencies in the file have been updated and verified.